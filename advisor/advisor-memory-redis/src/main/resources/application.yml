server:
  port: 8080

spring:
  application:
    name: advisor-memory-redis

  ai:
    openai:
      api-key: ${DASHSCOPE_API_KEY}
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-max

    memory:
      redis:
        host: localhost
        port: 6379
        timeout:  5000
        password: